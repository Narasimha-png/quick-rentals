import { CatalogSearchPipe } from './catalog-search.pipe';

describe('CatalogSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CatalogSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
